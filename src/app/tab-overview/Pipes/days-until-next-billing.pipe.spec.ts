import { DaysUntilNextBillingPipe } from './days-until-next-billing.pipe';

describe('DaysUntilNextBillingPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysUntilNextBillingPipe();
    expect(pipe).toBeTruthy();
  });
});
