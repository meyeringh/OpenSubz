import { DaysUntilNextCancelationPeriodDeadlinePipe } from './days-until-next-cancelation-period-deadline.pipe';

describe('DaysUntilNextCancelationPeriodDeadlinePipe', () => {
  it('create an instance', () => {
    // const pipe = new DaysUntilNextCancelationPeriodDeadlinePipe();
    // expect(pipe).toBeTruthy();
  });
});
